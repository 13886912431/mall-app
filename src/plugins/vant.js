import Vue from "vue";
import { 
    Button, 
    Tabbar,
    TabbarItem,
    Icon,
    Image,
    Loading,
    Lazyload,
    Empty,
    PullRefresh,
    List,
    Stepper
} from "vant";

Vue.use(Button)
    .use(Tabbar)
    .use(TabbarItem)
    .use(Icon)
    .use(Image)
    .use(Loading)
    .use(Lazyload)
    .use(Empty)
    .use(PullRefresh)
    .use(List)
    .use(Stepper);
